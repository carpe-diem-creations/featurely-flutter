# Provenance of the ARB catalogs in this directory
#
# Derived from the canonical Featurely string catalog:
#   repo:   featurely-app
#   path:   packages/locales/catalog/*.arb
#   commit: 6fb58ba7091ee66a1341c689ce446451adce866a
#   synced: 2026-08-10
#
# Incremental sync 2026-09-16 (In-App Chat): the 16 `sdk.chat.*` keys were
# copied from packages/locales/exports/flutter/app_<locale>.arb on the
# featurely-app commit b8b3e0a (In-App Chat). All other sdk.* keys were verified unchanged apart
# from the `#` normalization below.
#
# Incremental sync 2026-09-16 (Locales): 8 new locales — bg, el, fi, id, lt,
# ro, sk and sq — were vendored from packages/locales/catalog/<locale>.arb on
# the featurely-app commit 77790d0 (feat(locales): add bg, el, fi, id, lt, ro,
# sk and sq), cross-checked key-for-key against
# packages/locales/exports/flutter/app_<locale>.arb, with the transform below
# applied unchanged. SUPPORTED_LOCALES is now 33 (the original 25, then bg, el,
# fi, id, lt, ro, sk, sq). The SDK-local extension keys listed below were
# authored for these 8 locales in the style of their new upstream catalogs.
#
# Incremental sync 2026-09-16 (Traditional Chinese): featurely_zh_Hant.arb
# (@@locale zh_Hant) was vendored from packages/locales/catalog/zh-Hant.arb on
# the featurely-app commit 94945cd (feat(locales): add Traditional Chinese
# (zh-Hant)), cross-checked against exports/flutter/app_zh_Hant.arb, with the
# transform below applied unchanged; the SDK-local extension keys were
# authored in its Taiwan-standard style. SUPPORTED_LOCALES is now 34
# ('zh-Hant' last), and resolveLocale honors script subtags (mirrored in
# lib/src/l10n/locale_resolution.dart).
#
# Incremental sync 2026-09-17 (Chat day separators): `sdkChatToday` and
# `sdkChatYesterday` were copied for all 34 locales from
# packages/locales/exports/flutter/app_<locale>.arb on the featurely-app
# commit 27dc585 (feat(locales): add Today/Yesterday chat day-separator
# strings), with their @-metadata on the
# en template. All other sdk.* keys were verified unchanged.
#
# Transform (manual, per the SDK spec — no sync tooling in v1):
#   - only `sdk.*` keys are copied (email.* keys are server-side);
#   - key names are camelCased mechanically: strip the dots, capitalize each
#     segment boundary (sdk.form.emailHelper -> sdkFormEmailHelper);
#   - @-metadata and ICU placeholders are preserved on the en template;
#   - inside plural messages, ICU `#` is normalized to the named placeholder
#     (`one {# vote}` -> `one {{count} vote}`) because Flutter gen-l10n does
#     not substitute `#`; this is ICU-equivalent and lossless;
#   - locale ids use `_` separators for Flutter (pt-BR -> pt_BR).
#
# SDK-local extension keys (NOT in the canonical catalog yet — pending
# upstreaming; translations were authored here in the style of the upstream
# catalogs and should be replaced by canonical ones once the keys land):
#   sdkFormAttachError         (spec: "couldn't attach" screenshot rejection)
#   sdkFormEmailError          (spec: invalid_email surfaced as a field error)
#   sdkDetailCommentsDisabled  (spec: 403 comments_disabled stale-config notice)
#   sdkCommonRateLimited       (spec: 429 "try again in a moment" state)
#   sdkFilterShowResultsOverflow  (filter apply label when the count preview
#                                  page was full and more results exist)
#
# To re-sync after a canonical catalog change: repeat the transform above for
# all 34 locales, re-add the extension keys (until upstreamed), update the
# commit hash and date here, then run `flutter gen-l10n`.
